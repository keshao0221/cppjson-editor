### 学生管理系统（基础收尾）

-   **当前状态**：即将完成，支持增删改查、排序、文件持久化与加载。
-   **做到什么程度即可**：功能可用，数据能正确保存和恢复，代码结构清晰，但不需追求任何性能或架构优化。将其视作 C++ 基本语法与 STL 的练习终点。
-   **完成后行动**：整理代码并上传至 GitHub，然后立即转向下一个项目。

###  JSON 解析器（分两个版本）

####  基础版（大一暑假前完成）

-   **怎么做**：用 C++ 从零实现，不依赖第三方库。手写递归下降解析器，构建一个 DOM 树（每个节点可以是对象、数组、字符串、数字、布尔、null），实现格式化打印。
-   **做到什么程度**：
    -   能正确解析合法的 JSON 字符串并生成树。
    -   支持嵌套对象和数组，键值输出格式对齐。
    -   内部数据存储使用 `std::string`，不要求零拷贝。
    -   不必处理所有非法 JSON 的详细报错，但基本错误（如缺少引号、括号不匹配）要能捕获并给出提示。
    -   代码量约 500~800 行，作为理解递归下降与指针操作的敲门砖。

####  高性能零拷贝版（大二上集成到 HTTP 服务器时升级）

-   **怎么做**：将 `string` 存储全面替换为 `std::string_view`，直接指向原始输入缓冲区。解析器不再拥有数据内存，只维护视图。增加严格的错误处理与错误位置报告。尽可能消除解析过程中的内存分配和拷贝。
-   **做到什么程度**：
    -   能在不拷贝输入数据的前提下，正确从一块内存区域（例如 HTTP 接收缓冲区）中解析出 JSON 结构。
    -   单线程解析速度至少达到 RapidJSON 的 50% 以上，并能通过 `benchmark` 对比证明。
    -   能精准报告错误发生的位置（行号和列号）。

###  HTTP 服务器（核心项目，分四个阶段）

####  基础版（大一暑假第1周）

-   **怎么做**：使用 Linux Socket API（socket、bind、listen、accept），为每个连接创建一个线程处理。在线程内使用阻塞 I/O 读取 HTTP 请求行，解析 GET 路径，根据路径返回不同的静态 HTML 内容或 404。
-   **做到什么程度**：
    -   在浏览器访问 `http://localhost:8080/index.html` 能显示页面。
    -   同时打开多个页面访问，服务器不会崩溃（但性能低下是正常的）。
    -   具备最基础的错误处理（如 bind 失败、文件不存在）。
-   **停止条件**：功能跑通，不追求并发性能，不处理 POST、CGI 等。

####  Reactor 版（大一暑假第2-3周）

-   **怎么做**：抛弃多线程模型，使用 epoll （或 kqueue）实现单线程事件循环。将所有 socket 设为非阻塞，监听 `EPOLLIN` 和 `EPOLLOUT` 事件。读取请求后注册写事件，分批发送响应。实现简单的读写缓冲区管理。
-   **做到什么程度**：
    -   单线程下能同时维持数千个空闲连接，而不会像线程版那样资源暴涨。
    -   使用 `nc` 或 `telnet` 测试，输入 GET 请求后能正确响应。
    -   能解决粘包问题（请求可能分多次到达），有简单的状态机记录每个连接的解析阶段。
    -   用 Webbench 等工具压测时，QPS 明显高于基础版。

####  生产版（大一暑假第4-6周）

-   **怎么做**：
    -   将 Reactor 升级为主从 Reactor：主线程只做 accept，然后将连接分发给多个工作线程，每个工作线程运行自己的事件循环。
    -   实现通用的线程池与任务队列，支持异步提交。
    -   为连接对象和缓冲区实现定长内存池，减少频繁 `malloc/free`。
    -   集成定时器（时间轮或最小堆），定期检查并踢掉超时不活跃的连接。
    -   将零拷贝 JSON 解析器集成进来，实现一个 `/api/json` 接口，直接从网络缓冲区解析 JSON 并返回。
-   **做到什么程度**：
    -   使用 `wrk -t4 -c1000 -d30s` 压测静态页面，QPS 稳定在 10,000 以上，P99 延迟 < 100ms。
    -   压测 JSON API 接口时吞吐量无明显下降。
    -   长时间运行（如 24 小时）无内存泄漏（用 Valgrind 或 AddressSanitizer 验证）。
    -   有完整的 Makefile、README 和性能测试数据对比图表。

####  AI 推理服务网关扩展（大二上学期）

-   **怎么做**：在 HTTP 服务器上增加 SSE（Server-Sent Events）支持，能逐块推送数据。实现“动态 Batching”：后端用一个条件变量等待，积攒够多个请求后统一调度执行（使用 sleep 模拟推理延迟），然后将结果分别返回。
-   **做到什么程度**：
    -   客户端能通过一个流式接口（如 `/v1/chat/completions`）接收到分块返回的模拟大模型输出。
    -   在并发请求下，Batching 机制能显著提升整体吞吐量，并能从日志中观察到批次大小。
    -   接口设计完全模仿 OpenAI 风格，可作为独立开源项目展示。

###  新闻聚合器 / 模型下载器（大一暑假后期或大二上）

####  多线程新闻聚合器（基础版，快速过）

-   **怎么做**：一个线程池从若干个固定 URL 抓取内容，另一个线程负责打印或简单汇总，练习 `std::thread`、`mutex`、`condition_variable` 和生产者-消费者模型。
-   **做到什么程度**：能正确协同工作，无明显内存错误，理解并发同步的基本范式。完成这个版本后即停止，不必深挖。

####  高性能模型下载器（AI 方向升级替代）

-   **怎么做**：实现一个多线程分块下载工具，支持断点续传（记录进度文件）、限速、大文件完整性校验（SHA256）。使用 HTTP Range 请求分块，利用已开发的 HTTP 服务器或 libcurl 等发起请求。
-   **做到什么程度**：
    -   能稳定下载 GB 级大文件（如从 Hugging Face 下载模型权重），网络中断后可续传不全量重下。
    -   下载完成后自动校验哈希，确保文件完整。
    -   下载速度能跑满带宽，多线程协同无明显瓶颈。

###  CUDA 编程实践（大二寒假）

####  矩阵乘法 Kernel 开发与优化

-   **怎么做**：
    -   从最简单的 1D/2D grid 矩阵乘开始，正确计算结果。
    -   使用 shared memory 进行分块优化，引入线程协作。
    -   使用 nvprof 或 Nsight Systems 分析全局内存访问、bank conflict 等问题，进一步调整 tile 大小和加载模式。
-   **做到什么程度**：
    -   优化后的 kernel 相比初版有 3~5 倍以上的加速。
    -   能清晰地解释为何 shared memory 能减少访存延迟，以及 bank conflict 是如何产生的。
    -   代码运行在不同矩阵规模下均稳定正确。

####  阅读与扩展

-   **怎么做**：阅读 darknet 或 tiny-dnn 中卷积层、池化层的 CUDA 实现，理解工业级代码的结构。尝试为它们添加一个简单的 CUDA 激活函数算子（如 ReLU）。
-   **做到什么程度**：算子能正确集成进原有推理流程，输出数值与 CPU 版本完全一致。

###  极简 AI 推理引擎 / AI 编译器 Pass（大二下学期，二选一）

####  方案 A：极简 AI 推理引擎

-   **怎么做**：用 C++ 从 ONNX 文件或自定义格式中读取一个简单的模型（如 MLP 或 ResNet-18），构建计算图，实现内存规划（张量生命周期分析），调用 CUDA kernel 或 cuBLAS 执行各层算子。完全从零手写，不依赖 PyTorch 等大型框架。
-   **做到什么程度**：
    -   能加载预训练权重，在单张图片上运行推理，输出分类结果与 PyTorch 一致。
    -   支持 batch 输入，性能随 batch size 增大有所提升。
    -   拥有基础的内存和显存管理，不会在多次推理后出现显存泄漏。
    -   在 GitHub 上可编译运行，文档清晰。

####  方案 B：AI 编译器 Pass 开发

-   **怎么做**：基于 MLIR 或 TVM 教程，编写一个自定义编译 Pass。例如：识别特定的卷积模式并替换为高效的实现，或进行算子融合（Conv + BatchNorm + ReLU）。使用从 PyTorch 导出的模型作为输入，执行你的 Pass 并验证性能。
-   **做到什么程度**：
    -   编译后的模型推理速度相比原始导出模型有可测量提升（哪怕 5%~10%）。
    -   能解释 Pass 的工作原理，以及在计算图中的改写逻辑。
    -   代码可以作为一个编译栈的插件或模块，附有测试用例。

##  融入 AI 系统方向的具体调整

-   **不做纯算法**，而是聚焦 **AI 基础设施 / AI 系统**。
-   **HTTP 服务器 → 高性能推理服务网关**：支持流式输出、动态 Batching、优先级调度。
-   **JSON 解析器 → 大模型 API 协议层**：强化零拷贝与高性能，直接对接 Chat API。
-   **新闻聚合器 → 模型下载器 / RAG 预处理管线**：多线程分块下载、断点续传、文档解析与分块。
-   **大二关键补充**：系统学习 CUDA 编程，理解 Transformer 推理过程，培养硬件感知的优化意识。


### 详细时间规划

#### 大一暑假前（当前至 6 月）

-   **任务**：收尾学生管理系统（够用即可）；实现 JSON 解析器基础版（递归下降，支持合法 JSON 解析）；开始阅读《深入理解计算机系统》（CS:APP）前 3 章。
-   **工具准备**：搭建 Linux 开发环境，熟练使用 gcc/g++/gdb/valgrind，所有代码传至 GitHub。

#### 大一暑假（7 月–8 月）

-   **项目**：完成 HTTP 服务器的基础版→Reactor 版→生产版（含线程池、内存池、定时器、零拷贝 JSON），压测达标，撰写完整 README。
-   **同步理论学习**：精读 CS:APP 关键章节（优化程序性能、存储器层次、异常控制流、系统级 I/O、网络编程、并发编程），每学一章落到代码优化上。
-   **推荐书**：《Linux/UNIX 系统编程手册》（参考细节）、《C++ Primer》第 5 版（查缺补漏）。

#### 大二上学期（9 月–次年 1 月）

-   **学业**：冲刺高绩点，数据结构、计组、操作系统等核心课争取 90+，用项目经验在课程设计中拉开差距。
-   **项目升级**：将 HTTP 服务器改造为 AI 推理服务网关，增加 SSE 流式输出、请求 Batching，接口模拟 OpenAI 风格，独立建仓库。
-   **开源贡献**：向 Envoy、Nginx、vLLM 等大型项目提交 PR，从“good first issue”入手，目标至少 1–2 个被合并的 PR。

#### 大二寒假（1 月–2 月）

-   **CUDA 系统学习**：跑通 NVIDIA 官方 CUDA 编程指南，实现带 shared memory 优化的矩阵乘法 Kernel，并安装 nvprof/Nsight 进行 profiling；阅读 darknet 或 tiny-dnn 源码，尝试添加简单 CUDA 算子。
-   **推荐书**：《CUDA C 编程权威指南》。

#### 大二下学期（2 月–7 月）

-   **独立项目**：二选一 —— 极简 AI 推理引擎（C++ 实现 ONNX 解析、内存规划、CUDA 算子）或 AI 编译器 Pass（基于 MLIR/TVM 实现模型优化）。目标：跑通 ResNet-18 并输出正确结果。
-   **竞赛**：参加 ASC 超算竞赛、华为云 AI 系统大赛等个人/团队赛，追求获奖。
-   **必看网课**：CMU 15-418/15-618（并行计算）、Stanford CS149、MIT 6.172；B 站 OneFlow/MegEngine 架构分享。

#### 大二暑假（7 月–8 月）

-   **成果固化**：将全部项目（HTTP 服务器、推理网关、CUDA 算子库、AI 推理引擎）整理为作品集，附上性能数据和 GitHub 链接。
-   **“云实验室”经历**：申请 NVIDIA DLI 认证；继续给 PyTorch/vLLM 提交高质量 PR，若暑假能合并 2 个 PR，等同远程实习。
-   **套磁导师**：用作品集和 PR 记录，向目标院校 AI 系统/高性能计算方向老师发自荐信。

#### 大三及以后

-   **保研路线**：全力以赴夏令营文书和机试/面试，项目经验成为核心竞争力；可尝试将技术总结成短文学术报告上传 arXiv。
-   **考研路线**：若绩点边缘，大三上开始启动数学与 408 复习，因前期积累效率极高；项目暂停，但已足够复试。

## 6. 持续学习资源清单

### 必读书单

1.  《深入理解计算机系统》（CS:APP）
2.  《C++ Primer》第 5 版 +《Effective C++》
3.  《Linux/UNIX 系统编程手册》
4.  《CUDA C 编程权威指南》
5.  《计算机体系结构：量化研究方法》（进阶）

### 开源代码阅读顺序

-   cJSON（已读）→ 理解树结构与递归
-   libuv / libevent → 事件循环与 Reactor 生产实现
-   LevelDB → LSM-Tree 与存储引擎
-   vLLM 调度器 → Continuous Batching 机制
-   TVM / MLIR 教程库 → AI 编译器 Pass 结构

### 公开课与视频

-   CMU 15-213（CS:APP 配套）
-   MIT 6.S081（操作系统，xv6）
-   CMU 15-445（数据库系统）
-   Stanford CS149（并行计算）
-   陈天奇的“MLIR 与 AI 编译”公开课（B 站）
-   OneFlow、MegEngine 等官方技术分享（B 站）